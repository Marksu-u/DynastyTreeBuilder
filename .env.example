NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Shared Supabase project (same values as the other Bag of Holding tools)
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=""
SUPABASE_SECRET_KEY=""

NEXT_PUBLIC_AUTH_COOKIE_DOMAIN=

# Shared Postgres instance; both apps ship the same public schema
DATABASE_URL=""
DIRECT_URL=""

NEXT_PUBLIC_GA_ID=""

# Production only: set identically in both apps to .bagofholdingtools.com.
# Leave empty for localhost and preview deployments.
NEXT_PUBLIC_AUTH_COOKIE_DOMAIN=""
